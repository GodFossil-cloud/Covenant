/* TEMP TEST: tab-weld disabled to isolate iOS Safari dock hop.
   If the hop changes, the culprit is per-frame tab welding writes.
   Revert this commit to restore original behavior.
*/

(() => {
  'use strict';
  // Intentionally no-op.
})();
